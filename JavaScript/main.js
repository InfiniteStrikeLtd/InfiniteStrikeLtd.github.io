
// when the document (website) is ready run a unamed 
$(document).ready(function(){
    //when you click on IE_Close (The x) do the below
   $('#IE_Close').click(function(){
    //$('#IE_Close').remove();
    // remove the element form the document
    $('#IE_Waring').remove();   
   })
});